server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass ${API_UPSTREAM};
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
