apiVersion: v1
kind: Service
metadata:
  name: {{ include "testkube-samples.componentName" (dict "root" . "component" "web") }}
  labels:
    {{- include "testkube-samples.labels" (dict "root" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
      appProtocol: http
  selector:
    {{- include "testkube-samples.selectorLabels" (dict "root" . "component" "web") | nindent 4 }}
